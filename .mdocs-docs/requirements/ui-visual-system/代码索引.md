# UI 视觉体系 — 代码索引

> 本需求定位点（不含代码正文）。色板变量定义在 `src/web/styles/global.css` 的 `:root`，本需求**不改**它。

## 选中态与布局分层

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/web/app/App.css` | `.mdocs-tree-row.active` | 浅灰底 `#f2f3f5` + 左侧 3px `--mdocs-accent` 指示条 |
| `src/web/app/App.css` | `.mdocs-tree-row--graph-focus` | 图谱焦点，紫色系，**独立于** `.active` |
| `src/web/app/App.css` | `.mdocs-tree-row` | 内边距 `6px 12px` |
| `src/web/app/App.css` | `.mdocs-sidebar` | 背景 `#fafafa` |
| `src/web/app/App.css` | `.mdocs-main` | 背景 `#ffffff` |
| `src/web/app/DocumentTree.tsx` | `indent = { paddingLeft: 8 + depth * 16 }` | 树缩进（本次未改） |

## 浮层层级

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/web/app/App.css` | `.mdocs-dialog-backdrop` | **`z-index: 200`**（原 40）；手机端被侧边栏遮挡的修复点 |
| `src/web/app/App.css` | `.mdocs-layout--reader .mdocs-sidebar--drawer-open` | 手机侧边栏抽屉 `z-index: 50` |
| `src/web/app/App.css` | `.mdocs-layout--reader .mdocs-mobile-nav-scrim` | 抽屉遮罩 `z-index: 45` |
| `src/web/app/App.css` | `.mdocs-ai-write-overlay` | `z-index: 1200`（模态全屏层） |
| `src/web/app/App.css` | `.mdocs-agent-mermaid-float` | `z-index: 12000`（最高） |
| `src/web/app/App.css` | `.mdocs-agent-panel` | `z-index: 201` |

层级序与新增浮层的取值约定见 [设计契约·规则 4](./设计契约.md#规则-4浮层层级必须高于常驻层)。

## 控件声量

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/web/styles/global.css` | `button.primary` / `.secondary` / `.ghost` / `.danger` | 四档样式基线 |
| `src/web/app/DocChrome.tsx` | 收藏按钮 `className="ghost ..."` | 原 `secondary` |
| `src/web/app/DocChrome.tsx` | 评论按钮 `className="ghost ..."` | 原 `secondary` |
| `src/web/app/GraphPage.css` | `.graph-btn-ghost` / `.graph-btn-primary` / `.graph-btn-danger` | 图谱工具栏三档 |

## 空白状态

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/web/app/App.tsx` | `.mdocs-welcome` 区块 | 无文档焦点时的引导页 |
| `src/web/app/App.tsx` | `Code2`（lucide import） | 大号低透明度引导图标 |
| `src/web/app/App.css` | `.mdocs-welcome-icon` | 图标容器 |
| `src/web/i18n/locales/*.ts` | `createDocToStart` | 引导文案（复用既有 key，未新增） |

## 相关需求

| 需求 | 关联点 |
|------|--------|
| [knowledge-graph](../knowledge-graph/设计契约.md) | 图谱工具栏分区 / 手机端收纳 |
| [html-documents](../html-documents/设计契约-doc-chrome.md) | 编辑·预览分段控制器；手机 DocChrome 折叠 |
| [login-register-dialog](../login-register-dialog/设计契约.md) | 登录弹框形态 |
