# 异步任务队列 — 代码索引

> 本需求相关的代码定位点。

## 框架层：task-queue

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/server/task-queue/types.ts` | `TaskQueue`, `TaskScheduler`, `BackgroundTask`, `LogEntry` | 核心类型定义 |
| `src/server/task-queue/TaskQueue.ts` | `class TaskQueue` | 队列 + 调度核心实现 |
| `src/server/task-queue/log-store.ts` | `class TaskLogStore` | 日志文件读写（JSON Lines） |
| `src/server/task-queue/index.ts` | `taskQueue` | 单例导出 |
| `src/server/task-queue/TaskQueue.test.ts` | — | 单元测试 |

## 图谱接入

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/server/documents/graph-lifecycle.ts` | `GraphLifecycle`, `createGraphLifecycle` | 图谱具名生命周期接口 + 实现 |
| `src/server/documents/graph-task.ts` | — | 图谱任务定义（注册到 taskQueue） |
| `src/server/documents/graph.service.ts` | `createGraphDeps`, `buildTreeNodeForDoc` | 新增导出，供 graph-task 使用 |

## 接口层

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/server/routes/graph.routes.ts` | `buildGraphRouter` | 图谱路由：analyze 改为异步入队，新增 `GET /tasks/:taskId` |
| `src/web/services/endpoints.ts` | `analyzeGraphApi`, `getGraphTaskApi`, `GraphTaskStatus` | 前端 API 类型与函数 |

## 前端

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/web/app/GraphPage.tsx` | `GraphPage` | 图谱页面：异步进度面板 + 轮询 |
| `src/web/app/GraphPage.css` | `.graph-progress-*` | 进度面板样式 |
