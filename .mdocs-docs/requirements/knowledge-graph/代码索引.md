# 知识图谱 — 代码索引

> 本需求定位点（不含代码正文）。功能尚未开发，以下为预期触达点，实现后补充精确符号与行号。

## 数据模型

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/shared/graph.ts` | `GraphNode` / `GraphEdge` / `SourceRef` | 共享类型定义（待新增） |

## 后端 — 存储与服务

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/server/storage/file-store.ts` | `readGraphFile` / `writeGraphFile` | `___graph___.json` 读写（待新增） |
| `src/server/documents/graph.service.ts` | — | 图谱聚合、去重、权限过滤（待新增） |
| `src/server/routes/graph.routes.ts` | `buildGraphRouter` | GET 图谱、触发分析（待新增） |

## 后端 — AI 分析

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/server/agent/Agent/tools-graph.ts` | `createGraphTools` | 图谱分析 Agent tools（待新增） |
| `src/server/agent/Agent/graph-analyzer.ts` | `analyzeDirectoryGraph` | 目录图谱分析主流程（待新增） |

## 前端

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/web/app/GraphPage.tsx` | `GraphPage` | 图谱页面（待新增） |
| `src/web/services/endpoints.ts` | `fetchGraphApi` / `triggerGraphAnalyzeApi` | API 客户端（待新增） |

## 文档树过滤

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/shared/docPath.ts` | `isSpecialFile` | `___graph___.json` 标记为特殊文件（待更新） |
