# 知识图谱 — 代码索引

> 本需求定位点（不含代码正文）。2026-08-26 dirty 补丁相关入口如下。

## 核心构建

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/server/documents/graph/index.ts` | `buildGraph` / `buildDocumentGraph` / `buildFolderGraph` / `aggregateAndInduce` | 自底向上构建；本补丁改为看 `meta.dirty` |
| `src/server/documents/graph/types.ts` | `Graph` / `GraphDeps` /（待补）`GraphFile` | 内存图 vs 落盘 envelope |
| `src/server/documents/graph/utils.ts` | `makeNodeId` / `findRootNodes` / `needRebuild` | id / 入度 0；`needRebuild` 将被 dirty 取代为主路径 |
| `src/server/documents/graph.service.ts` | `createGraphDeps` / `buildDomainGraph` / `readDirGraph`… | IO 适配、域构建 |

## 本补丁（dirty / 发布 / 生命周期）

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/server/documents/graph/change-ratio.ts` | `changeRatio`（待新增） | 两段正文行级变化率 |
| `src/server/documents/graph/publish-hook.ts` | 发布后异步评估（待新增） | 无缓存跳过；超阈值 dirty + 祖先蔓延 |
| `src/server/documents/graph.service.ts` | `markAncestorsDirty` 等（待新增） | 沿 `parent_id` 上爬置目录/域图 dirty；删文章级缓存 |
| `src/server/documents/document.service.ts` | `removeDocument` / `moveDocument` / 删目录 / 发布路径 | 挂生命周期与 publish-hook |
| `src/server/documents/lexical-text.ts` | `extractPlainTextFromLexical` | 发布比较用正文提取 |

## API / 前端

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/server/routes/graph.routes.ts` | `buildGraphRouter` | GET / analyze |
| `src/web/app/GraphPage.tsx` | `GraphPage` | 分层可见子图 + 深度工具栏 + 角标展开 |
| `src/web/app/graph-layered-view.ts` | `buildContainsHierarchy` / `computeVisibleIds` | contains 骨架与可见集 |
| `src/web/services/endpoints.ts` | graph API | 客户端 |

## 特殊文件名

| 路径 | 符号 | 说明 |
|------|------|------|
| `src/shared/graph-files.ts` | `DIR_GRAPH_FILENAME` / `ARTICLE_GRAPH_DIRNAME` / `articleGraphFileName` | 隐藏路径约定 |
| `src/shared/file-types.ts` | `FILE_TYPE.GRAPH_FILE` / `GRAPH_DIR` | 文档树过滤 |
